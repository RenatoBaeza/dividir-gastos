# Pinned, not floating: the deployment installs these at build time, and a
# `>=` here means production quietly runs whatever shipped that morning.
# Bump deliberately, then run the test suite.
fastapi==0.141.1
uvicorn==0.52.4
sqlalchemy==2.0.52
psycopg[binary]==3.3.5
pydantic==2.13.5
pydantic-settings==2.15.0
pyjwt[crypto]==2.13.0
httpx==0.28.1
python-dotenv==1.2.3
email-validator==2.3.0
